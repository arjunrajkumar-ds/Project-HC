# Add sys.argv for -v/--view & -a/--add & -r/--report

# --report: Runs calculation for today's date

# --add: Takes & stores input of regularly eaten foods & macronutrient breakdown. Option to add sugar value

import os

DATA_FILE = os.path.join("data", "food_database.json")